#import "./Component.graphql"
#import "./Prop.graphql"

mutation addComponent($input: AddComponentInput!) {
  addComponent(input: $input) {
    ... ComponentInfo
    props {
      ... PropInfo
    }
  }
}
