#import "./Provider.graphql"

query providers($limit: Int!, $after: Int) {
  providers(limit: $limit, after: $after) {
    totalCount
    edges {
      cursor
      node {
        ... ProviderInfo
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
